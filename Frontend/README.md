# Eye Care ERP - Frontend

React + TypeScript + Vite + Zustand frontend for Eye Care Institute Management System.

## Quick Start

```bash
# Install dependencies
npm install

# Copy environment file
cp .env.example .env

# Start development server
npm run dev
```

## Environment Variables

Create a `.env` file:

```env
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_APP_NAME=Eye Care ERP
VITE_APP_VERSION=1.0.0
```

## Project Structure

